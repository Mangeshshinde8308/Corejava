package com.demo.interfacedemo1;

public interface InterfDemo {
	
	int m=10;  //we need to initialized  variable. 
	//int n;  -->>compile time error if not inintiallised
	//private int n=10;  -->private is invalid modifier for instance variable.
	//We cannot define constructor to interface. because all variable inside interface needs to be initialized.
	//Interface is 100% abstarction/abstract class.

}
